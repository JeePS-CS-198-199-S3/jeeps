import 'package:flutter/cupertino.dart';

class PropertiesSettings extends StatelessWidget {
  const PropertiesSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
